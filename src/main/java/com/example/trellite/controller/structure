AuthController  (/auth)
├── POST   /register          → Register user
├── POST   /login             → Login user
└── POST   /logout            → Logout user  ← missing

BoardController  (/boards)
├── GET    /                  → Get all boards         ← missing
├── POST   /                  → Create board           (was /create)
├── GET    /:boardId          → Get a specific board   (was /${board_name})
├── PATCH  /:boardId          → Update board info
├── PATCH  /:boardId/close    → Close board            (was no route)
├── DELETE /:boardId          → Delete board           (was /delete)
├── GET    /:boardId/members  → See board members/owner ← was "see board owner"
├── POST   /:boardId/members  → Add member to board    ← missing
└── DELETE /:boardId/members/:memberId  → Remove member ← missing

ListController  (/boards/:boardId/lists)   ← ENTIRELY MISSING
├── GET    /                  → Get all lists on a board
├── POST   /                  → Create a list
├── PATCH  /:listId           → Rename / update a list
└── DELETE /:listId           → Delete a list

TaskController  (/boards/:boardId/lists/:listId/tasks)
├── GET    /                  → View all tasks in a list
├── POST   /                  → Add a task
├── GET    /:taskId           → View a single task
├── PATCH  /:taskId           → Modify a task
├── DELETE /:taskId           → Remove a task
├── PATCH  /:taskId/assign    → Assign task to a user  (was "see who task assigned to")
└── PATCH  /:taskId/move      → Move task to another list ← missing